{
  "name": "echo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "biome check --error-on-warnings .",
    "lint:fix": "biome check --write --error-on-warnings ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "husky": "^9.1.7"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "vite": "^8.0.16",
    "ws": "^8.21.0"
  }
}
